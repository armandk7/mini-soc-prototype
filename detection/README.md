# Detection Rules

Validated rules in the lab:

- `100100` — successful SSH login
- `100102` — command execution
- `100103` — brute-force correlation

The XML files are placeholders until the exact validated rule definitions are copied from the lab configuration. Do not invent or modify rule logic for the portfolio without retesting it.
