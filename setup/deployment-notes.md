# Deployment Notes

This document is intentionally kept high-level for the public portfolio.

## Components

- Kali Linux attacker VM
- Cowrie honeypot
- Wazuh manager
- Wazuh custom detection rules
- Active Response
- Linux `iptables`

## Publication rule

Only publish configuration that is safe to disclose. Remove or redact credentials, tokens, private keys, internal-only host information, and other secrets before committing.
